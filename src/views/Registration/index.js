import React from 'react';
import Layout from "../../Layout";

const Registration = () => {
    return (
        <Layout>
            Registration
        </Layout>
    );
};

export default Registration;